package com.nob.validation.context;

import com.nob.validation.metadata.context.AttributeMetadata;

public class ValueBasedAttribute extends AbstractAttribute implements Attribute {

    public ValueBasedAttribute(AttributeMetadata attributeMetadata) {
        super(attributeMetadata);
    }
}
